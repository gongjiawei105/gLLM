{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "FastAPI (project root)",
            "type": "debugpy",
            "request": "launch",
            "module": "uvicorn",
            "args": [
                "src.server:app"
            ],
            "cwd": "${workspaceFolder}",
            "envFile": "${workspaceFolder}/src/.env",
            "env": {
                "PYTHONPATH": "${workspaceFolder}"
            }
        }
    ]
}